import {
    Dimensions,
    Platform
} from "react-native";
var { height, width } = Dimensions.get('window')

export const COLOR = {
    PRIMARY: '#449D47',
    LINE: '#e3e3e3'
}

export const SCREEN = {
    WIDTH: width,
    HEIGHT: height
}